name: Deploy Crate

on:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

runs-on: ubuntu-latest

jobs:
  release:
    steps:
    - uses: actions/checkout@v2
    - name: Install cargo-release
      run: cargo install cargo-release
    - name: Release
      run: cargo-release release --config release.toml
